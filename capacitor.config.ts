import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scruffy.oneSignal',
  appName: 'oneSignal',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
